class CommentsController {
  constructor() {
    this.$addCommentForm = $('.add-comment')

  }
  init() {
    // kick off controller from here
    $('#user-text').click(function()
}

CommentsController.prototype.addCommentFormListener() {
  // create comment form listener code here
  id="add.comment"
  document.getElementById("#user-text")
}

CommentsController.prototype.render(commentObject) = function(image) {
  $('#images').append(image.imageEl(comments.{this.imageID}));
}

render(commentId) {
 document.getElementById("commentId")
}
//iterates through each comment form and adds an eventlistener to trigger a function on form submit
