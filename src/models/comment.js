// create Comment class here
class Comment {
	constructor(comment, imageId) {
	this.id = id
	this.comment = comment,
	this.imageId = imageId
	}

	newComment() {
		let newComment = new Comment(comment, imageId)
	}

	static all(){
		var comments = [] 
		comments.push(this.new)
	}

	findImage(imageId) {
		document.getElementById("#${imageId}")
	}



}
